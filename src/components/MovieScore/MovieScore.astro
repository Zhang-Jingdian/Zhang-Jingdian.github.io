---
import MovieScoreClient from './MovieScoreClient.vue'

interface Props {
  theme?: 'default' | 'dark'
}

const { theme = 'default' } = Astro.props
---

<MovieScoreClient theme={theme} client:load />
